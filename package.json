{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "postinstall": "prisma generate",
    "vercel-build": "npm install && npm run build"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "next": "^14.0.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "next-auth": "^4.0.0"
  },
  "prisma": {
    "schema": "prisma/schema.prisma"
  }
}
